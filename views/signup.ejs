<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <!-- CDN -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css"
        integrity="sha512-Kc323vGBEqzTmouAECnVceyQqyqdsSiqLQISBL29aUW4U/M7pSPA/gEUZQqv1cwx4OnYxTxve5UMg5GT6L4JJg=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea, #764ba2);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .logic-part {
            padding: 30px;
        }

        .login-container {
            display: grid;
            grid-template-columns: 50% 50%;
            /* background-color: white; */
            background: linear-gradient(135deg, #7f94f3, #bc84f3);
            box-shadow: 0px 0px 30px;
            animation: fadeIn 1s ease-in-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-50px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        button {
            margin-left: 110px;
            margin-top: 10px;
            padding: 8px 30px;
            background-color: #48c6ef;
            background-image: linear-gradient(135deg, #6f86d6 0%, #48c6ef 100%);
            border: none;
            border-radius: 10px;
            cursor: pointer;
            font-size: 18px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        button:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.3);
        }

        .register-link {
            margin-left: 65px;
            margin-top: 17px;
            display: block;
            color:#072bf8;
            /* text-decoration: none; */
            font-size: 18px;
            transition: color 0.3s ease;
        }

        .register-link:hover {
            color: rgb(241, 7, 7);
            /* text-decoration: underline; */
        }

        input {
            border: 2px solid blueviolet;
            padding: 8px;
            width: 300px;
            margin: 10px;
            outline: none;
        }

        input:focus {
            background-color: #e5d5fa;
            letter-spacing: 1px;
        }

        ::placeholder {
            font-size: 15px;
        }

        /* p{
            color: #764ba2;
            font-weight: bold;
        } */
        h1 {
            font-size: 35px;
            margin: 0;
        }
         label{
            font-weight: bold;
         }
        form {
            margin-top: 30px;
        }
    </style>
</head>

<body>
    <div class="login-container">
        <div class="img-part">

            <img src="/images/signup-img.png" alt="">
        </div>
        <div class="logic-part">
            <h1>Sign Up</h1>
            <!-- <p>Login to continue</p> -->

            <form action="/auth/signup" method="POST">
                <div>
                    <label for="username"><i class="fa-solid fa-user"></i> Username:</label><br>
                    <input type="text" id="username" name="username" placeholder="Enter your username" required>
                </div>
                <div>
                    <label for="password"><i class="fa-solid fa-lock"></i> Password:</label><br>
                    <input type="password" id="password" name="password" placeholder="Enter your password" required>
                </div>
                <button type="submit">Sign Up</button>
            </form>
            <!-- Link for new users to register -->
            <a href="/" class="register-link">Have an Account? Login</a>
        </div>


    </div>
</body>

</html>