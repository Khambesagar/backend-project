<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <!-- CDN Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css"
        integrity="sha512-Kc323vGBEqzTmouAECnVceyQqyqdsSiqLQISBL29aUW4U/M7pSPA/gEUZQqv1cwx4OnYxTxve5UMg5GT6L4JJg=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }

        body {
            background: linear-gradient(rgba(210, 139, 252, 0.9), rgba(110, 196, 230, 0.9));
            width: 100%;
            height: auto;
        }


        .section {
            display: flex;
            justify-content: center;
            margin: 50px 0;
            width: auto;
            height: auto;
        }

        #t-head {
            background-color: #1B1464;
            color: white;
            letter-spacing: 1px;
            z-index: 1;
            position: relative;
        }

        .loading {
            /* animation: fadeIn 5s ease-in-out; */
            z-index: 0;
            position: static;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-300px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeInforall {
            from {
                opacity: 0;
                transform: translateY(-50px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .container {
            animation: fadeInforall 2s ease-in-out;
            display: grid;
            flex-wrap: wrap;
            justify-content: center;
            align-items: center;
            align-content: center;
            width: auto;
            height: auto;
            padding: 30px;
            background-color: #eaecec;
            box-shadow: 0px 0px 12px black;
            background: linear-gradient(rgba(156, 26, 231, 0.9), rgba(23, 173, 233, 0.9));

        }

        .main {
            padding: 30px 0;
        }

        .navbar {
            width: 100%;
            background-color: #1a0069;
        }

        nav {
            padding: 15px 25px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        nav a {
            color: white;
            font-size: 20px;
            text-decoration: none;
        }

        .logout-btn:hover {
            color: #ffae00;
        }

        .W-title {
            color: #FF8F00;
        }

        .D-title {
            text-align: center;
            color: white;
        }

        .centered-border {
            border: none;
            height: 3px;
            background-color: white;
            width: 30%;
            margin: 0 auto;
        }



        .form-1 {
            text-align: center;
        }

        .buttons {
            display: flex;
            justify-content: space-between;
        }

        .dashboard-btn,
        .add-btn {
            border: 1px solid;
            padding: 5px 15px;
            margin: 8px 0;
            background-color: #6A1B9A;
            color: white;
            text-decoration: none;
            border-radius: 10px;
        }

        .dashboard-btn:hover,
        .add-btn:hover {
            color: white;
            background-color: #880E4F;
            border-color: #FF6F00;
            transition: 0.6s;
        }

        .serch-box {
            width: 200px;
            padding: 8px 5px;
            background-color: #bdc3c7;
            font-weight: bold;
            border-radius: 7px;
            border: 2px solid;
        }

        .serch-box:hover {
            border-color: #e84118;
            transition: 0.3s ease-in;
        }

        .search-btn {
            padding: 8px;
            font-weight: bold;
            color: white;
            background-color: #455A64;
            border-radius: 7px;
        }

        .search-btn:hover {
            background-color: #1B1464;
            transition: 0.3s ease-in;
        }

        table {
            border-radius: 15px;
        }

        /* #t-head {
            background-color: #1B1464;
            color: white;
            letter-spacing: 1px;
            z-index: 0;
        } */

        #t-head th {
            padding: 8px 0;
        }

        .btn-del,
        .btn-upd {
            border: none;
        }

        .fa-trash-can {
            font-size: 19px;
            color: red;
            transition: transform 0.3s ease;
        }

        .fa-pen-to-square {
            color: green;
            margin: 0 10px;
            font-size: 20px;
            transition: transform 0.3s ease;
        }

        .fa-trash-can:hover,
        .fa-pen-to-square:hover {
            transform: translateY(-3px);
        }

        td {
            padding: 10px;
            font-weight: bold;

        }

        .btn {
            cursor: pointer;
        }

        tr:nth-child(even) {
            background-color: #b5cce7;
            color: black;
            /* Light gray for even rows */
        }

        tr:nth-child(odd) {
            background-color: #d8e6f7;
            color: black;
            /* White for odd rows */
        }

        tr:nth-child(even):hover,
        tr:nth-child(odd):hover {
            /* background-color: #0288D1; */
            background-color: rgb(3, 211, 226);
            color: white;
        }
    </style>
</head>

<body>
    <div class="navbar">
        <nav>
            <h1 class="W-title btn"><i class="fa-solid fa-database"> </i> Employee Database System</h1>
            <a href="/" class="btn logout-btn"><i class="fa-solid fa-user btn"></i> Logout</a>
        </nav>
    </div>

    <div class="section">
        <div class="container">

            <h1 class="D-title">Admin Dashboard</h1>
            <hr class="centered-border">
            <div class="main">
                <div class="one">
                    <form action="/search" method="GET" class="form-1 btn">
                        <span><input type="text"  name="_id" placeholder="Enter User ID" class="serch-box btn" required>
                            <button type="submit" class="search-btn btn">Search</button></span>
                    </form>
                    <div class="buttons">
                        <a href="/dashboard" class="dashboard-btn btn">Go to Dashboard</a>
                        <a href="/add-data" class="add-btn btn">Add User</a>
                    </div>
                </div>
                <table border="1" cellspacing="0" cellpadding="7px"
                    style="table-layout:auto; width:900px;  overflow: hidden; ">
                    <thead>
                        <tr id="t-head">
                            <th>ID</th>
                            <th>Name</th>
                            <th>Position</th>
                            <th>Salary</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>

                        <% data.forEach(user=>{ %>
                            <tr class="loading">
                                <td>
                                    <%= user._id %>
                                </td>
                                <td>
                                    <%= user.name %>
                                </td>
                                <td>
                                    <%= user.position %>
                                </td>
                                <td>
                                    <%= user.salary %>
                                </td>

                                <td>
                                    <a href="/update/<%= user._id %>" class="btn-upd btn"><i
                                            class="fa-solid fa-pen-to-square"></i></a>

                                    <!-- <form action="/delete/<%= user._id%>" method="post"
                                        style="display:inline-block; margin: 0; padding: 0;">
                                        <button type="submit" class="btn-del btn" onclick=onclick="return confirmDelete();">
                                            <i class="fa-solid fa-trash-can"></i></button>
                                    </form> -->
                                    <form action="/delete/<%= user._id %>" method="post" style="display:inline-block; margin: 0; padding: 0;">
                                        <button type="submit" class="btn-del btn" onclick="return confirmDelete();">
                                            <i class="fa-solid fa-trash-can"></i>
                                        </button>
                                    </form>
                                    
                                </td>
                            </tr>
                            <% }) %>

                    </tbody>
                </table>
            </div>
        </div>
    </div>
    <script>
        function confirmDelete() {
            return confirm("Are you sure you want to delete this data? This action cannot be undo.");
        }
    </script>
    
    
</body>

</html>